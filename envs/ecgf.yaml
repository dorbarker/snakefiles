channels:
        - bioconda
dependencies:
        - python=3.7
        - git
        - blast
        - biopython
        - numpy
        #- pip:
        #        - "--editable=git+https://github.com/dorbarker/CGFPrediction.git@improvements"
